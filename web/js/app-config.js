// ============================================================================
// EDIT THIS FILE ONLY when you have a new Android app release link.
// Nothing else in this project needs to change for a new release —
// paste the new GitHub release URL below, replace this one file on GitHub,
// and every "Get the app" link across the site updates automatically.
//
// The APK/AAB itself is now built by .github/workflows/android-build.yml
// (Tauri, not Capacitor — see README "Why Tauri, not Capacitor"). Run that
// workflow, download its APK artifact (or attach it to a GitHub Release the
// same way the old Capacitor build was distributed), and paste that URL
// below.
// ============================================================================

export const ANDROID_APP_DOWNLOAD_URL = 'https://github.com/darshandpatel63-prog/DD-COMPRESSER-PRIVACY-FIRST-COMPRESSER/releases/latest/download/DD-Compressor.apk';


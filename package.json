{
  "name": "dd-compressor",
  "version": "1.0.0",
  "private": true,
  "description": "DD Compressor — privacy-first file compression, on your device, on web and Android",
  "scripts": {
    "tauri": "tauri",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build:debug": "tauri android build --apk --debug",
    "android:build:apk": "tauri android build --apk",
    "android:build:aab": "tauri android build --aab",
    "icons": "tauri icon icon-source/app-icon-1024.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
